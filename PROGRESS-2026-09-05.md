## 2026-09-05 — Unlimit Music v2 UI fixes

Today's session worked on the guitar chord + scales wheel app (`/workspaces/hermes/greg/projects/unlimit-music/`).

### Changes made

**Scales wheel:**
- Added counter-rotation so orb labels (text + highlight) stay upright while the ring spins
- Replaced inner rim + turn-hint with a visible 3px ring border OUTSIDE the orbs as the drag surface
- Removed all arrows / turn-hint elements
- Made wheel wider (`clamp(240px, 34vh, 380px)`) for more drag room
- Clustered orbs at 30% of wheel radius so there's a wide draggable band outside the orb ring
- Fixed snap: `onUp()` no longer kills `dragging` before `inertia()` could run
- Center letter stays upright (child of `wheelEl`, not `ringEl`)
- Note name persists after finger lifts (center not cleared); fades out after 3s
- First note appears instantly (removed CSS opacity transition)
- Glissando lights: only currently touched orb is lit
- Octave stepping: `pickOctave` sweeps ±3 octaves, tracks pitch direction from last two taps
- Persistent pitch tracking across taps (same continuity as glissando, finger just lifted)
- Key change resets pitch tracking (so new root starts fresh)
- Same-note taps reuse exact same midi pitch (doesn't shift octave on repeated taps)

**Top bar layout:**
- Settings and Scale buttons pushed to corners with `justify-content: space-between`
- Removed duplicate `.ad-slot` CSS rule that was collapsing the ad banner
- Ad has `min-width: 200px` now
- Chord buttons don't shrink too small (min-height 28px, font-size clamp .85rem)

**AdSense:**
- Publisher ID set: `ca-pub-1921965753258324`
- W-8BEN tax form approved (UK resident, 0% withholding)
- Auto Ads configured (no specific slot yet)

### Remaining
- Piano diagram slot (reserved in HTML, not built)
- Site footer shows but ad banner currently has no live AdSense code (Auto Ads stub only)

## 2026-09-05 (cont.) — Content pages + mobile

Added content pages for AdSense approval:
- `chords.html` — Chord Library (major, minor, 7th, sus, extended, altered, add chords)
- `scales.html` — Guitar Scales (major, minor, pentatonic, blues, modes)
- `theory.html` — Music Theory Basics (keys, circle of fifths, progressions, capo, power chords)
- `learn.html` — How chords are built (intervals, triads, 7ths, sus, extended)

Mobile responsiveness:
- Fixed to use `transform: scale(0.7)` so everything stays in place and just shrinks proportionally (no rearranging)
- Removed all the stacking/snapping mobile rules that were moving things around

Zip handoff: `http://localhost:8766/share/unlimitmusic-v2.zip`
