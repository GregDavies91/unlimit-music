// Site config — fill in your own values. Safe to commit (no secrets here).
window.SITE_CONFIG = {
  // Your Google AdSense publisher ID, e.g. "ca-pub-1234567890123456".
  // Get this from AdSense after approval: Account > Sites > your site > Ad unit code.
  ADSENSE_PUB_ID: "ca-pub-1921965753258324",

  // Optional: a specific display ad unit slot ID from your AdSense ad unit,
  // e.g. "1234567890". If left as-is, the site uses AdSense AUTO ADS
  // (Google places ads automatically — no slot needed). Either way, ads
  // only appear AFTER AdSense approves unlimitmusic.com.
  ADSENSE_AD_SLOT: "REPLACE_WITH_AD_UNIT_SLOT",

  // The domain you bought. Used for the consent notice + meta.
  SITE_NAME: "Unlimit Music",
  SITE_DOMAIN: "unlimitmusic.com",

  // NOTE ON BING / MICROSOFT ADVERTISING:
  // Bing Ads does NOT put ads on your site — it is for BUYING traffic
  // (you pay to send visitors to unlimitmusic.com). Turn that on later,
  // after the site is indexed and earning, from ads.microsoft.com.
};
